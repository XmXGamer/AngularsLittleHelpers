import { AuthorizeRouteConfiguration } from './authorize-route-configuration';

describe('AuthorizeRouteConfiguration', () => {
  it('should create an instance', () => {
    expect(new AuthorizeRouteConfiguration()).toBeTruthy();
  });
});
